% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd.R
\name{dd}
\alias{dd}
\title{Score text with distributional density metrics}
\usage{
dd(text, dictionary, data = NULL, id = NULL, bound = c("finite", "continuous"))
}
\arguments{
\item{text}{Either a character vector of texts, or, if \code{data} is
supplied, a single string naming the column of \code{data} that holds
the text.}

\item{dictionary}{As in \code{\link[=distributional_density]{distributional_density()}}: a character
vector for a single category, or a named list of character
vectors to score several categories at once. Category names
become column-name prefixes when more than one category is
scored.}

\item{data}{Optional data frame to append scores to. If supplied,
\code{text} (and \code{id}, if given) must name columns of \code{data}, and the
return value is \code{data} with score columns appended. If omitted,
\code{text} is used directly as the vector of texts and a new data
frame is built from it.}

\item{id}{Optional text identifiers, used to align scores back onto
\code{data}. If \code{data} is supplied, this can be a single string naming
an id column in \code{data}. Defaults to row order, or to \code{names(text)}
if \code{data} is not supplied and \code{text} is a named vector.}

\item{bound}{As in \code{\link[=dd_dispersion]{dd_dispersion()}}: \code{"finite"} (default) or
\code{"continuous"}.}
}
\value{
A data frame: \code{data} with score columns appended if \code{data}
was supplied, otherwise a new data frame with \code{id}, \code{text}, and
score columns.
}
\description{
Shorthand, data.frame-friendly wrapper around
\code{\link[=distributional_density]{distributional_density()}}. Where \code{distributional_density()}
returns a long table (one row per text-by-category combination),
\code{dd()} returns one row per text, with the four distributional
density parameters appended as columns -- either onto a data frame
you already have, or onto a new one built from a plain character
vector of texts.
}
\examples{
texts <- c(
  "For me, the best part of the day is the morning coffee.",
  "The best part of the day, for me, is the morning coffee."
)
self_words <- c("i", "me", "my", "mine", "myself")

# vector in, new data frame out
dd(text = texts, dictionary = self_words)

# existing data frame in, same data frame out with scores appended
df <- data.frame(id = c("p1", "p2"), text = texts, stringsAsFactors = FALSE)
dd(text = "text", data = df, id = "id",
   dictionary = list(self = self_words, time = c("day", "morning")))
}
