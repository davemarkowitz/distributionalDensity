% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributional_density.R
\name{distributional_density}
\alias{distributional_density}
\title{Compute distributional density metrics for text}
\usage{
distributional_density(
  text,
  dictionary,
  id = NULL,
  bound = c("finite", "continuous"),
  standardize = FALSE,
  n_sim = 1000
)
}
\arguments{
\item{text}{A character vector of texts. Can be a single string or
many; each is scored independently.}

\item{dictionary}{Either a character vector of words/stems
defining a single category (e.g. \code{c("i", "me", "my", "mine", "myself")}), or a named list of such vectors to score multiple
categories at once (e.g. \code{list(self = c("i", "me", ...), negemo = c("sad", "angry", ...))}). Entries ending in \code{"*"} match by
prefix (e.g. \code{"happi*"} matches "happy", "happiness", "happier");
matching is otherwise exact and case-insensitive.}

\item{id}{Optional vector of text identifiers, recycled to
\code{length(text)}. Defaults to \code{1:length(text)}, or to \code{names(text)}
if \code{text} is named.}

\item{bound}{Which upper bound to normalize dispersion against; see
\code{\link[=dd_dispersion]{dd_dispersion()}}. Defaults to \code{"finite"}.}

\item{standardize}{If \code{TRUE}, the \code{burstiness} column holds a
z-score against a simulated finite-size null instead of the raw
Kim & Jo value, removing burstiness's residual dependence on
occurrence count (and so on text length); see
\code{\link[=dd_burstiness]{dd_burstiness()}}. \code{burstiness_raw} is never standardized.
Default \code{FALSE}.}

\item{n_sim}{Number of null placements to simulate per
text-by-category combination when \code{standardize = TRUE}. Default
1000.}
}
\value{
A data frame with one row per text-by-category combination
and columns \code{id}, \code{category}, \code{n_words}, \code{n_events}, \code{prevalence},
\code{burstiness}, \code{burstiness_raw}, \code{position}, \code{dispersion}, and
\code{dispersion_continuous}. \code{burstiness} is \code{NA} for categories with
fewer than 3 occurrences in a text, and \code{dispersion}/
\code{dispersion_continuous} are \code{NA} for fewer than 2.
}
\description{
Scores one or more texts against one or more word-list categories
and returns all four distributional density parameters --
prevalence, burstiness, position, and dispersion -- for each
text-by-category combination.
}
\examples{
texts <- c(
  "For me, the best part of the day is the morning coffee.",
  "The best part of the day, for me, is the morning coffee.",
  "The best part of the day is the morning coffee, for me."
)
self_words <- c("i", "me", "my", "mine", "myself")
distributional_density(texts, self_words)

distributional_density(
  texts,
  list(self = self_words, time = c("day", "morning"))
)
}
